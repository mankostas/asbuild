import { test, expect } from '@playwright/test';

test('workflow designer placeholder', async () => {
  // Backend is not available in CI; this placeholder ensures test suite runs.
  expect(true).toBe(true);
});
